# DebateAI Environment Configuration
# Copy this file to .env and fill in your values

# ===========================================
# Database Configuration (Required)
# ===========================================
DATABASE_URL=mysql://user:password@localhost:3306/debate_ai

# ===========================================
# Authentication (Required)
# ===========================================
JWT_SECRET=your-jwt-secret-here
OAUTH_SERVER_URL=https://your-oauth-server.com
OWNER_OPEN_ID=your-owner-open-id

# ===========================================
# Application Settings
# ===========================================
VITE_APP_ID=debate-ai
NODE_ENV=development

# ===========================================
# Speech-to-Text Configuration
# ===========================================
# Option 1: Forge API (Primary - if available)
# BUILT_IN_FORGE_API_URL=https://forge.manus.im
# BUILT_IN_FORGE_API_KEY=your-forge-api-key

# Option 2: Hugging Face API (Fallback or Primary)
# Get your API key from: https://huggingface.co/settings/tokens
HUGGINGFACE_API_KEY=hf_your_api_key_here

# Optional: Specify which Whisper model to use
# Available models:
#   - openai/whisper-large-v3 (highest accuracy, slowest)
#   - openai/whisper-large-v3-turbo (recommended - fast with good accuracy)
#   - openai/whisper-small (balanced)
#   - openai/whisper-base (fast, lower accuracy)
#   - openai/whisper-tiny (fastest, lowest accuracy)
HUGGINGFACE_MODEL=openai/whisper-large-v3-turbo

# Force use of Hugging Face even if Forge is available
# USE_HUGGINGFACE=true

# ===========================================
# LLM Configuration (for AI features)
# ===========================================
# Uses the same Forge API for LLM features
# BUILT_IN_FORGE_API_URL and BUILT_IN_FORGE_API_KEY are used

# ===========================================
# Notes
# ===========================================
# 1. Speech-to-text will automatically use the best available provider:
#    - If Forge API is configured, it will be used as primary
#    - If only Hugging Face is configured, it will be used
#    - If both are configured, Forge is primary with HF as fallback
#
# 2. For Hugging Face, the first request may take 20-30 seconds
#    while the model loads into memory
#
# 3. Supported audio formats: mp3, wav, webm, ogg, m4a
#    Maximum file size: 16MB (Forge) or 25MB (Hugging Face)
